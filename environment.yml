name: hcad_pred
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.7
  - setuptools
  - wheel
  - invoke
  - notebook
  - jupyter_contrib_nbextensions
  - requests
  - numpy
  - matplotlib
  - papermill
  - pandas
  - pip
  - pip:
    - -e .
    - python-dotenv>=0.5.1